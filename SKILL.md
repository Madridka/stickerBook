# SKILL.md — справочник проекта

Этот документ описывает знания, необходимые для безопасного развития «Вклейки», и
показывает, где в проекте находится соответствующая логика. Обязательные правила кода
зафиксированы в [`rules/rules.md`](rules/rules.md). Рабочий процесс находится в
[`AGENTS.md`](AGENTS.md), обнаруживаемый навык Codex — в
[`.agents/skills/stickerbook-development/SKILL.md`](.agents/skills/stickerbook-development/SKILL.md).
Этот корневой файл остаётся справочником, а не вторым автоматически загружаемым навыком.

## Продуктовая область

Разработчик должен понимать полный игровой цикл: заработок голов, расход энергии,
покупку и открытие блистеров, выпадение карточек, хранение оригиналов и повторок,
подготовку и вклейку наклеек, цели, ежедневные задания, pity-механики и пики 1 из 5.
Изменение одной части экономики необходимо оценивать вместе с сохранением прогресса,
облачным объединением и отображением ресурсов в интерфейсе.

## Основные технические навыки

- Vue 3 Composition API и `<script setup>`: views, небольшие компоненты, composables,
  реактивные вычисления и управление жизненным циклом.
- TypeScript в строгом режиме: публичные интерфейсы, discriminated unions, type-only
  imports и явные типы публичных функций.
- Pinia setup stores: локальное состояние домена, вычисляемые значения и атомарные
  действия без option stores.
- Dexie.js и IndexedDB: последовательные миграции схемы, транзакции, индексы и
  сохранение незавершённых игровых операций.
- vue-i18n: все пользовательские тексты и форматируемые числовые значения находятся
  в `src/lang/{locale}/`.
- TailwindCSS и PrimeVue 4: адаптивный интерфейс, рассчитанный на Full HD, 720p и
  мобильный viewport 390×844.
- Fastify и SQLite: cookie-сессии, версионированные облачные сохранения, OpenAPI,
  админские маршруты и резервное копирование.
- Контентные пайплайны: типизированные каталоги карточек, JSON-данные, WebP-ресурсы и
  стабильные публичные URL.

## Карта архитектуры

| Область                   | Основные каталоги и файлы                                |
| ------------------------- | -------------------------------------------------------- |
| Экраны и маршруты         | `src/views/`, `src/router/`                              |
| Переиспользуемый UI       | `src/components/`                                        |
| Реактивные сценарии       | `src/composables/`                                       |
| Состояние доменов         | `src/stores/`                                            |
| Экономика и параметры     | `src/config/`                                            |
| Каталоги журналов         | `src/data/`                                              |
| Локальное сохранение      | `src/db/`                                                |
| Облачная синхронизация    | `src/services/cloudSave.ts`, `src/services/saveMerge.ts` |
| Цели и ежедневные задания | `src/features/goals/`, `src/features/dailyTasks/`        |
| Сервер и админка          | `server/`                                                |
| Скрипты данных и защиты   | `scripts/`                                               |
| Локализация               | `src/lang/`                                              |

## Где проверять инварианты

- Баланс: `src/config/gameBalance.ts`, `src/services/economy.ts`.
- Выдача карточек, оригиналы и повторки: `src/db/stickerLifecycle.ts`.
- Схема сохранения: `src/db/database.ts`.
- Объединение снимков: `src/services/saveMerge.ts`, `tests/unit/saveMerge.spec.ts`.
- История получения наград: `src/features/goals/goalRewardService.ts`,
  `src/features/dailyTasks/dailyTaskService.ts`.
- Доступность альбомов и каталогов: `src/data/albumRegistry.ts`,
  `src/data/cardCatalogLoader.ts`, `scripts/runtime-catalog.test.ts`.

## Порядок внесения изменений

Выбрать затронутый домен по карте выше и использовать соответствующий сценарий
проектного навыка. Матрица проверок — в `AGENTS.md`, требования к версии и
документации — в `rules/rules.md`. Не перечитывать несвязанные домены при локальной правке.
